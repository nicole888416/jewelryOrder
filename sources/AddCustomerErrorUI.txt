package controller.customer;

import java.awt.*;
import java.awt.event.*;
import javax.swing.*;
import javax.swing.border.EmptyBorder;

public class AddCustomerErrorUI extends JFrame {

	private static final long serialVersionUID = 1L;
	private JPanel contentPane;
	
	
	private final Color BRAND_PINK = new Color(230, 128, 128);
	private final Color PANEL_WHITE = new Color(255, 255, 255, 200);
	private final Font BELL_FONT_TITLE = new Font("Bell MT", Font.BOLD | Font.ITALIC, 26);
	private final Font BELL_FONT_BODY = new Font("Bell MT", Font.PLAIN, 15);

	/**
	 * Launch the application.
	 */
	public static void main(String[] args) {
		EventQueue.invokeLater(() -> {
			try {
				AddCustomerErrorUI frame = new AddCustomerErrorUI();
				frame.setVisible(true);
			} catch (Exception e) {
				e.printStackTrace();
			}
		});
	}

	/**
	 * Create the frame.
	 */
	public AddCustomerErrorUI() { /
		setTitle("Jewellery Store - Registration Notice");
		setDefaultCloseOperation(JFrame.EXIT_ON_CLOSE);
		setBounds(100, 100, 650, 400); 
		
		contentPane = new JPanel(null);
		contentPane.setBackground(BRAND_PINK);
		setContentPane(contentPane);

		// --- 1. 左側內容區：提示訊息 ---
		JPanel infoPanel = new JPanel(null);
		infoPanel.setBackground(PANEL_WHITE);
		infoPanel.setBounds(50, 50, 320, 260);
		contentPane.add(infoPanel);

		JLabel lblTitle = new JLabel("Membership Notice");
		lblTitle.setHorizontalAlignment(SwingConstants.CENTER);
		lblTitle.setFont(BELL_FONT_TITLE);
		lblTitle.setBounds(10, 40, 300, 40);
		infoPanel.add(lblTitle);

		// 轉化為優雅的提示語
		JLabel lblMsg = new JLabel("<html><center>The username is already<br>in our prestigious records.</center></html>");
		lblMsg.setHorizontalAlignment(SwingConstants.CENTER);
		lblMsg.setFont(BELL_FONT_BODY);
		lblMsg.setForeground(new Color(80, 80, 80)); 
		lblMsg.setBounds(10, 100, 300, 50);
		infoPanel.add(lblMsg);

		// 按鈕
		JButton btnBack = new JButton("TRY ANOTHER");
		btnBack.setFont(new Font("Bell MT", Font.BOLD, 14));
		btnBack.setBackground(Color.BLACK);
		btnBack.setForeground(Color.WHITE);
		btnBack.setFocusPainted(false);
		btnBack.setBorderPainted(false); 
		btnBack.setOpaque(true);
		btnBack.setBounds(70, 180, 180, 40);
		btnBack.setCursor(new Cursor(Cursor.HAND_CURSOR));
		infoPanel.add(btnBack);

		// --- 2. 右側區域：廣告留位 (Ad Area) ---
		JPanel adPanel = new JPanel(null);
		adPanel.setBounds(400, 0, 250, 400);
		adPanel.setBackground(new Color(0, 0, 0, 60)); 
		contentPane.add(adPanel);

		JLabel lblAdTitle = new JLabel("NEW COLLECTION"); 
		lblAdTitle.setForeground(Color.WHITE);
		lblAdTitle.setFont(new Font("Bell MT", Font.ITALIC, 16));
		lblAdTitle.setHorizontalAlignment(SwingConstants.CENTER);
		lblAdTitle.setBounds(0, 180, 250, 30);
		adPanel.add(lblAdTitle);
		
		// 加入圖片
		ImageIcon adIcon = new ImageIcon(new ImageIcon("resources/Addcustomererror_Ad.jpg").getImage().getScaledInstance(250, 400, Image.SCALE_SMOOTH));
		JLabel lblAdImg = new JLabel(adIcon);
		lblAdImg.setBounds(0, 0, 250, 400);
		adPanel.add(lblAdImg);

		// --- 3. 邏輯處理 ---
		btnBack.addActionListener(e -> {
			new AddCustomerUI().setVisible(true); // 返回註冊頁面
			dispose();
		});

		setLocationRelativeTo(null); // 畫面置中
	}
}